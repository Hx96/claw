# 2026-07-20 (Sunday)

## 微信推送问题修复（延续）

- 用户反馈早上 cron 推送全部没收到，要求手动补发
- 三个 cron 任务（AI HOT 等）状态均显示 `delivered=true`，但微信端实际未展示
- 与 6/30、7/18 相同的老问题：短时间内多个 cron 密集执行（6:50~8:10 共 7 个），微信限流/折叠
- AI 手动补发了三份内容
- **改进措施：**
  - 重新分散了 cron 任务执行时间，避免短时间轰炸
  - 去掉了基于 grep outbound 日志的投递验证方案（不可靠，异步缓冲写入）
  - 改为使用 message 工具返回值中的 messageId 来验证投递成功
  - 端到端测试通过：`✅ 已验证投递 (messageId=openclaw-weixin:...)`
